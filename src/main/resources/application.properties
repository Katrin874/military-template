spring.application.name=military-service

# === ???????????? ???? ????? ===
# ????? ?????: ?????????????? 127.0.0.1, ??? ?????? ???????? ? DNS/Docker
spring.datasource.url=jdbc:postgresql://127.0.0.1:5435/military_db
spring.datasource.username=military_user
spring.datasource.password=military_pass
spring.datasource.driver-class-name=org.postgresql.Driver

# ???????????? HikariCP ??? ????????? ????????
spring.datasource.hikari.connection-timeout=60000 
spring.datasource.hikari.initialization-fail-timeout=120000

# === LIQUIBASE (????????) ===
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.xml
spring.liquibase.enabled=true
spring.liquibase.drop-first=false

# === HIBERNATE (???????????? ORM) ===
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# === ???????????? REDIS (???? 5: ?????????) ===
# ????? ?????: ?????????????? 127.0.0.1
spring.data.redis.host=127.0.0.1
spring.data.redis.port=6379
spring.data.redis.timeout=60000

# ???????????? ????
spring.cache.type=redis
spring.cache.redis.time-to-live=3600000

# === JWT SECURITY ===
application.security.jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
application.security.jwt.expiration=86400000

# ====================================
# === OBSERVABILITY (ETAP 7: ACTUATOR & METRICS) ===
# ====================================

# 1. ?????????? ??????????: ??????????? health, metrics, prometheus ?? info
management.endpoints.web.exposure.include=health,metrics,prometheus,info

# 2. ??????????? Health Check
management.endpoint.health.show-details=always

# 3. ????????? ??????? ? ??????? Prometheus (??? Grafana/Prometheus)
management.metrics.export.prometheus.enabled=true

# 4. Custom application info (???????? ????? /actuator/info)
info.app.name=Military Logistics System
info.app.version=1.0.0
info.app.stage=Production Ready